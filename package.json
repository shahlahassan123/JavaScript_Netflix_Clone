{"scripts": {
     "predeploy": "npm run build",
     "deploy": "gh-pages -d dist"
},
  "dependencies": {
    "axios": "^1.1.3",
    "swiper": "^8.4.5"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0"
  }
}
